export class Project {
    public name: string
    public desc: string
    public link: string
    public img: string
    constructor(name: string, desc: string, link: string, img: string) {
        this.name = name
        this.desc = desc
        this.link = link
        this.img = img
    }
}
